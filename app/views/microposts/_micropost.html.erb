<li id="micropost-<%= micropost.id %>" class="clearfix”>
  <%= link_to gravatar_for(micropost.user, size: 10), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user == micropost.user %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
    <%= link_to "super delete", micropost, method: :delete,
                                       data: { confirm: "Maybe it's other user's message, do you understand?" } %>
  </span>
</li>